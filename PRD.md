# InsightLoop 产品需求文档（PRD）

> 版本：v0.1（原型对齐版）
> 更新日期：2026-07-28
> 状态：原型已实现（单文件 Web 应用 + GitHub Pages 托管）；AI 模型为 Mock 占位，待接入真实模型。

---

## 1. 产品概述

InsightLoop 是一个面向 B 端产品团队的 **AI 产品迭代闭环工作台**，把"用户反馈 → 洞察 → 机会 → 上线效果"整合成一个可被 AI 贯穿的闭环。核心价值：让产品团队以更少的人工，把零散反馈转化为可验证的产品机会，并持续追踪上线效果。

## 2. 目标用户与场景

- **产品经理**：从反馈中识别高价值机会，生成 PRD、拆解任务、评估优先级。
- **产品负责人**：在洞察看板掌握全局趋势，在机会工作区推进机会落地。
- **运营 / 用研**：在反馈中心聚合多源反馈，标注情绪与主题。

## 3. 核心闭环

```
反馈中心（采集 / 标注）
   ↓
洞察看板（聚类 / 趋势）
   ↓
机会工作区（转化 / 生成 PRD / 拆解 / 评分）
   ↓
上线效果仪表盘（度量 / 复盘 / 下轮建议）
   ↓ （回到反馈中心，形成迭代闭环）
```

## 4. 功能模块（已设计 + 已实现 UI）

### 4.1 反馈中心
- 多源用户反馈汇聚；情绪标签、主题标签。
- AI 能力 `BATCH_ANALYZE`：一键触发多智能体分析，输出聚类摘要、情绪分布、高频主题。

### 4.2 洞察看板
- 基于多源反馈自动聚类的高频问题、影响范围、情绪趋势。
- AI 能力 `AI_REVIEW`：对洞察做可信度 / 影响面复核。

### 4.3 机会工作区（核心）
将高价值洞察转化为可验证产品机会（原型含 5 个机会，2 个推进中）。每个机会支持：
- `GENERATE_PRD`：生成产品需求文档（可编辑文本区）。
- `DECOMPOSE_TASKS`：拆解为可执行任务列表（`tasks[]`）。
- `AI_SCORE`：影响 / 价值 / 证据 / 成本多维度评分 + 综合分（`score`）。
- `GEN_ACCEPTANCE`：生成验收标准（`acceptance_criteria[]`）。
- `GEN_TRACKING`：生成上线追踪 / 埋点事件（`tracking_events[]`）。

### 4.4 上线效果仪表盘
- 上线后核心指标度量与对比。
- AI 能力 `AI_SUGGEST`：给出下轮迭代建议；支持"查看复盘"。

## 5. AI 能力契约（ai-contract.js）

所有 AI 调用统一走**固定 JSON 契约**，不返回自然语言段落：

- `buildContext`：强类型输入信封 + token 预算约束。
- `buildSystemPrompt`：硬性约束模型只输出符合 schema 的 JSON。
- `callModel`：失败状态机 —— `repairJson` → `validate(partial)` → 重试 / 降级 → 中文错误。
- `regenerate`：更换 `variationSeed` 重新采样，保留版本历史以逐版对比。
- 8 个能力 SCHEMAS：`BATCH_ANALYZE` / `GENERATE_PRD` / `AI_REVIEW` / `GEN_ACCEPTANCE` / `GEN_TRACKING` / `DECOMPOSE_TASKS` / `AI_SCORE` / `AI_SUGGEST`。

## 6. AI 状态与异常处理（三层展示法）

为统一表达 AI 生成状态（尤其是 loading / 失败 / 重生），定义三层展示法：

1. **内联状态徽标**：每个 AI 按钮旁显示 待生成 / 生成中 / 已生成 / 部分生成 / 生成失败（五色标准化）。
2. **Agent 追溯抽屉**：展开呈现模型、输入 / 输出 Token、耗时、重试次数、版本 seed。
3. **顶栏活动日志**：铃铛 + 下拉，汇总所有 AI 动作，带状态点（成功绿 / 失败红）+ 时间戳。

**失败与重生统一处理**：任意层失败 → 红色失败横幅（含左侧红条）+「重新生成」+「查看日志」；重新生成换 seed、历史可对比，避免静默失败。

> 设计系统规范（含上述三层 + 失败横幅视觉令牌）沉淀在 Ardot 画布「AI 协同状态规范」帧，作为设计—开发对齐基准；该规范页不进入终端用户 HTML。

## 7. 技术与部署（已实现）

- 单文件 Web 应用 `index.html`：1440px 等比缩放、纯 CSS / JS、零外部资源依赖（仅 Noto Sans SC 字体）。
- ES Module：`ai-integration.js`（前端联调 / 三层渲染）`import` `ai-contract.js`（契约层）。
- 模型层 `mockModel` 返回符合 schema 的 JSON 并走 `repairJson → validate` 完整链路；支持"模拟异常"分支（超时 / 脏 JSON）用于演示异常处理。
- 部署：GitHub Pages（`main` 分支根目录），访问 <https://besthqhd.github.io/insightloop-app/>。

## 8. 非目标（当前版本）

- 不接入真实大模型（当前为 Mock）。
- AI 协同状态规范页不进入终端用户 HTML（仅设计契约）。
- 不含账号体系 / 多租户 / 数据持久化（原型态）。

## 9. 验收标准

- [x] 四页 UI 与设计稿视觉一致（布局 / 配色 / 圆角 / 轻投影）。
- [x] 每个 AI 按钮接 ai-contract，返回固定 JSON 并被三层展示法正确消费。
- [x] 模拟异常下，失败横幅 + 重新生成 + 查看日志 全链路可用。
- [x] GitHub Pages 可公开访问原型。

## 10. 后续路线

- 接入真实模型（替换 `mockModel`，保留契约与三层展示法）。
- 机会工作区 PRD / 任务持久化与多人协作。
- 上线效果接入真实埋点数据。
